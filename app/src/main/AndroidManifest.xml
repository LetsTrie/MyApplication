<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sakib23.myapplication">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyProfile"
                  android:label="My Profile"  ></activity>
        <activity android:name=".PostDetails" />
        <activity android:name=".forum_question" />
        <activity
            android:name=".CreateNewArticle"
            android:label="Create An Article" />
        <activity
            android:name=".Forum"
            android:label="Forum" />
        <activity
            android:name=".SignupActivity"
            android:label="Create An Account" />
        <activity
            android:name=".Homepage"
            android:label="Homepage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>